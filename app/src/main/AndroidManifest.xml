<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.proshiftteam.proshift">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.NoActionBar">
        <activity android:name=".Activities.HomeActivity" android:exported="true"/>
        <activity android:name=".Activities.RegisterActivity" />
        <activity android:name=".Activities.ManagerControlsActivity"></activity>
        <activity android:name=".Activities.CurrentShiftAddRemoveActivity"></activity>
        <activity android:name=".Activities.SearchOpenShiftsActivity"></activity>
        <activity android:name=".Activities.RequestTimeOffActivity"></activity>
        <activity android:name=".Activities.CreateNewShiftActivity"></activity>
        <activity android:name=".Activities.ApproveShiftRequestActivity"></activity>
        <activity android:name=".Activities.ApproveTimeOffRequestActivity"></activity>
        <activity android:name=".Activities.ModifyShiftActivity"></activity>
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>